/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove static export for now to fix client-side navigation
  // output: 'export',
  // distDir: 'out',
};

module.exports = nextConfig;
